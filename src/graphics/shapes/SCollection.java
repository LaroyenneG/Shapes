package graphics.shapes;

public class SCollection extends Shape {
    public SCollection(Object newModel) {
        super(newModel);
    }

    public SCollection() {
        super(new Object());
    }
}
